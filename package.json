{
  "name": "preview-sample-app",
  "version": "1.0.0",
  "description": "Boring fixed sample app with a /health endpoint, used to test the preview platform.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
